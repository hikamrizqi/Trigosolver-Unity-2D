╔═══════════════════════════════════════════════════════════════════╗
║              HIGH SCORE PANEL - NAVIGATION FLOW                   ║
╚═══════════════════════════════════════════════════════════════════╝

📁 UI HIERARCHY
═══════════════════════════════════════════════════════════════════

Canvas/
├── LogoPanel
├── MainMenuPanel
│   ├── MulaiButton → OnMulaiClicked()
│   ├── ⭐ HighScoreButton → OnHighScoreClicked()  [NEW]
│   └── KeluarButton → OnKeluarClicked()
│
├── ModeSelectionPanel
├── ModeCeritaSelectionPanel
│
└── ⭐ HighScorePanel [NEW]
    ├── Background
    ├── Title ("HIGH SCORES")
    ├── ScoreContainer
    │   ├── Level1Row
    │   │   ├── Label ("Level 1:")
    │   │   └── ScoreText (TextMeshProUGUI)
    │   ├── Level2Row
    │   │   ├── Label ("Level 2:")
    │   │   └── ScoreText (TextMeshProUGUI)
    │   └── TotalRow
    │       ├── Label ("Total:")
    │       └── ScoreText (TextMeshProUGUI)
    │
    └── ⭐ BackButton → OnBackFromHighScore()  [NEW]

Components Required:
- HighScorePanel: MenuAnimationController + HighScoreDisplay
- Buttons: Button component with OnClick events

╔═══════════════════════════════════════════════════════════════════╗
║                    NAVIGATION FLOW DIAGRAM                         ║
╚═══════════════════════════════════════════════════════════════════╝

                    ┌─────────────┐
                    │ Logo Panel  │
                    └──────┬──────┘
                           │ Click anywhere / Auto
                           ▼
                    ┌─────────────┐
              ┌────▶│ Main Menu   │◀────┐
              │     │   Panel     │     │
              │     └──────┬──────┘     │
              │            │             │
              │  ┌─────────┼─────────┐  │
              │  │         │         │  │
              │  │ Mulai   │ High    │  │
              │  │         │ Score   │  │
              │  ▼         ▼         │  │
        ┌───────────┐  ┌──────────┐ │  │
        │   Mode    │  │  High    │ │  │
        │ Selection │  │  Score   │ │  │
        │   Panel   │  │  Panel   │◀┘  │
        └─────┬─────┘  └────┬─────┘    │
              │             │           │
              │ Back        │ Back      │
              └─────────────┴───────────┘

State Flow:
Logo → MainMenu → HighScore → MainMenu (loop)
            ↓
     ModeSelection → ModeCerita

╔═══════════════════════════════════════════════════════════════════╗
║                 ANIMATION SEQUENCE DETAILS                         ║
╚═══════════════════════════════════════════════════════════════════╝

🎬 OPENING HIGH SCORE PANEL:
═══════════════════════════════════════════════════════════════════

Step 1: User clicks "High Score" button
   │
   ├─► Check: currentState == MainMenu? ✅
   │
   └─► currentState = HighScore

Step 2: Main Menu Panel animates OUT
   │
   ├─► mainMenuAnimator.AnimateSinkOut()
   │   └─► Panel slides DOWN offscreen
   │
   └─► Duration: ~0.5s

Step 3: High Score Panel animates IN
   │
   ├─► highScoreAnimator.AnimateDropIn()
   │   └─► Panel drops FROM TOP
   │
   └─► Duration: ~0.5s

Step 4: Refresh scores (callback after animation)
   │
   └─► highScoreDisplay.RefreshScores()
       ├─► Get scores from HighScoreManager
       ├─► Update TextMeshProUGUI elements
       └─► Animate scores in (slide + scale)

Total Animation Time: ~1.0s


🎬 CLOSING HIGH SCORE PANEL (BACK):
═══════════════════════════════════════════════════════════════════

Step 1: User clicks "Back" button
   │
   ├─► Check: currentState == HighScore? ✅
   │
   └─► currentState = MainMenu

Step 2: High Score Panel animates OUT
   │
   ├─► highScoreAnimator.AnimateSinkOut()
   │   └─► Panel slides DOWN offscreen
   │
   └─► Duration: ~0.5s

Step 3: Main Menu Panel animates IN
   │
   ├─► mainMenuAnimator.AnimateDropIn()
   │   └─► Panel drops FROM TOP
   │
   └─► Duration: ~0.5s

Total Animation Time: ~1.0s


╔═══════════════════════════════════════════════════════════════════╗
║                  INSPECTOR ASSIGNMENTS                             ║
╚═══════════════════════════════════════════════════════════════════╝

🎮 MAINMENUMANAGER INSPECTOR:
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ Menu Panels                                                      │
├─────────────────────────────────────────────────────────────────┤
│ Logo Panel                   → LogoPanel                        │
│ Main Menu Panel              → MainMenuPanel                    │
│ Mode Selection Panel         → ModeSelectionPanel               │
│ Mode Cerita Selection Panel  → ModeCeritaSelectionPanel         │
│ ⭐ High Score Panel          → HighScorePanel                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ High Score Display                                               │
├─────────────────────────────────────────────────────────────────┤
│ ⭐ High Score Display        → HighScorePanel (component)       │
└─────────────────────────────────────────────────────────────────┘


🎨 HIGHSCOREDISPLAY INSPECTOR:
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ Score Text References                                            │
├─────────────────────────────────────────────────────────────────┤
│ Level1 Score Text    → HighScorePanel/.../Level1Row/ScoreText  │
│ Level2 Score Text    → HighScorePanel/.../Level2Row/ScoreText  │
│ Total Score Text     → HighScorePanel/.../TotalRow/ScoreText   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Animation Settings                                               │
├─────────────────────────────────────────────────────────────────┤
│ Animate On Enable      → ✅ True                                │
│ Animation Duration     → 0.5                                    │
│ Animation Delay        → 0.2                                    │
│ Normal Color           → White (255, 255, 255)                  │
│ Highlight Color        → Yellow (255, 255, 0)                   │
└─────────────────────────────────────────────────────────────────┘


🔘 BUTTON ONCLICK EVENTS:
═══════════════════════════════════════════════════════════════════

HighScoreButton (MainMenuPanel):
┌─────────────────────────────────────────────────────────────────┐
│ Button (Script)                                                  │
├─────────────────────────────────────────────────────────────────┤
│ OnClick()                                                        │
│   Runtime Only                                                   │
│   ┌───────────────────────────────────────┐                     │
│   │ MainMenuManager                       │                     │
│   │ Function: MainMenuManager             │                     │
│   │          .OnHighScoreClicked()        │                     │
│   └───────────────────────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘

BackButton (HighScorePanel):
┌─────────────────────────────────────────────────────────────────┐
│ Button (Script)                                                  │
├─────────────────────────────────────────────────────────────────┤
│ OnClick()                                                        │
│   Runtime Only                                                   │
│   ┌───────────────────────────────────────┐                     │
│   │ MainMenuManager                       │                     │
│   │ Function: MainMenuManager             │                     │
│   │          .OnBackFromHighScore()       │                     │
│   └───────────────────────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘


╔═══════════════════════════════════════════════════════════════════╗
║                      STATE MANAGEMENT                              ║
╚═══════════════════════════════════════════════════════════════════╝

MenuState Enum Values:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ State                │ Active Panel          │ Available Actions │
├──────────────────────┼──────────────────────┼──────────────────┤
│ Logo                 │ LogoPanel            │ Click anywhere   │
│ MainMenu             │ MainMenuPanel        │ Mulai, HighScore,│
│                      │                      │ Keluar           │
│ ModeSelection        │ ModeSelectionPanel   │ Mode Cerita,     │
│                      │                      │ Mode Bebas, Back │
│ ModeCeritaSelection  │ ModeCeritaPanel      │ Chapter 1, Back  │
│ ⭐ HighScore         │ HighScorePanel       │ Back             │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

State Transition Logic:
───────────────────────────────────────────────────────────────────
✅ Logo → MainMenu (auto/click)
✅ MainMenu → ModeSelection (Mulai button)
✅ MainMenu → HighScore (HighScore button) ⭐
✅ MainMenu → Quit (Keluar button)
✅ ModeSelection → MainMenu (Back button)
✅ ModeSelection → ModeCerita (Mode Cerita button)
✅ ModeCerita → ModeSelection (Back button)
✅ HighScore → MainMenu (Back button) ⭐

❌ HighScore → ModeSelection (not allowed)
❌ ModeSelection → HighScore (not allowed - must go through MainMenu)


╔═══════════════════════════════════════════════════════════════════╗
║                    CODE IMPLEMENTATION                             ║
╚═══════════════════════════════════════════════════════════════════╝

📝 MainMenuManager.cs - Key Methods:
═══════════════════════════════════════════════════════════════════

public void OnHighScoreClicked()
{
    // State guard: Only from MainMenu
    if (currentState != MenuState.MainMenu) return;

    // Update state
    currentState = MenuState.HighScore;

    // Animation sequence: Sink → Drop → Refresh
    mainMenuAnimator.AnimateSinkOut(() =>
    {
        highScoreAnimator.AnimateDropIn(() =>
        {
            // Refresh scores after panel appears
            if (highScoreDisplay != null)
            {
                highScoreDisplay.RefreshScores();
            }
        });
    });
}

public void OnBackFromHighScore()
{
    // State guard: Only from HighScore
    if (currentState != MenuState.HighScore) return;

    // Update state
    currentState = MenuState.MainMenu;

    // Animation sequence: Sink → Drop
    highScoreAnimator.AnimateSinkOut(() =>
    {
        mainMenuAnimator.AnimateDropIn();
    });
}


╔═══════════════════════════════════════════════════════════════════╗
║                      TESTING SCENARIOS                             ║
╚═══════════════════════════════════════════════════════════════════╝

Test Case 1: Normal Flow
───────────────────────────────────────────────────────────────────
1. Game starts → Logo shows
2. Click anywhere → Main Menu appears
3. Click "High Score" → High Score panel slides in
4. Verify: Scores display (or "---" if no data)
5. Click "Back" → Main Menu returns
✅ Expected: Smooth transitions, correct scores

Test Case 2: Rapid Clicks
───────────────────────────────────────────────────────────────────
1. Main Menu showing
2. Click "High Score" rapidly 5 times
✅ Expected: State guard prevents multiple transitions
✅ Expected: Animation only plays once

Test Case 3: Navigation Loop
───────────────────────────────────────────────────────────────────
1. Main Menu → High Score
2. Back → Main Menu
3. High Score → Back
4. Repeat 5 times
✅ Expected: No stuck states, smooth each time

Test Case 4: Score Display
───────────────────────────────────────────────────────────────────
1. Fresh install (no PlayerPrefs)
2. Open High Score panel
✅ Expected: Shows "---" for all scores
3. Play Level 1, get score 80
4. Return to Main Menu
5. Open High Score panel
✅ Expected: Shows "80" for Level 1
✅ Expected: Scores animate in (slide + scale)

Test Case 5: State Guards
───────────────────────────────────────────────────────────────────
1. Navigate to Mode Selection
2. Try to trigger OnHighScoreClicked() via inspector
✅ Expected: Nothing happens (state guard blocks)
3. Navigate to High Score panel
4. Try to trigger OnMulaiClicked() via inspector
✅ Expected: Nothing happens (state guard blocks)


╔═══════════════════════════════════════════════════════════════════╗
║                      TROUBLESHOOTING                               ║
╚═══════════════════════════════════════════════════════════════════╝

Problem: Button tidak respond
├─► Check: Button component enabled?
├─► Check: OnClick event assigned?
├─► Check: MainMenuManager reference not null?
└─► Check: EventSystem present in scene?

Problem: Panel tidak animate
├─► Check: MenuAnimationController attached?
├─► Check: highScoreAnimator not null?
├─► Check: Panel has RectTransform?
└─► Check: DOTween installed?

Problem: Scores tidak muncul
├─► Check: HighScoreDisplay component attached?
├─► Check: TextMeshProUGUI references assigned?
├─► Check: RefreshScores() called?
└─► Check: HighScoreManager initialized?

Problem: Wrong state transitions
├─► Check: currentState updated correctly?
├─► Check: State guards (if statement) working?
├─► Check: MenuState enum has HighScore value?
└─► Check: No direct SetActive() calls bypassing state?

Problem: Animation glitches
├─► Check: Animation duration not too fast/slow?
├─► Check: No conflicting animations running?
├─► Check: Callbacks properly chained?
└─► Check: Panel positions reset correctly?


═══════════════════════════════════════════════════════════════════
                            END OF GUIDE
═══════════════════════════════════════════════════════════════════

Created: December 22, 2025
Commit: 641a2ed
Feature: High Score Panel with Button Navigation
